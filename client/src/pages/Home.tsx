/**
 * Dotloop Reporting Tool - CSV Upload Page
 * Temporary clean version - Dotloop OAuth will be added in next phase
 */

import { Button } from '@/components/ui/button';

export default function Home() {
  return (
    <div className="min-h-screen bg-background flex items-center justify-center">
      <div className="text-center space-y-4">
        <h1 className="text-4xl font-bold">Dotloop Reporting Tool</h1>
        <p className="text-muted-foreground">
          Clean rebuild in progress...
        </p>
        <p className="text-sm text-muted-foreground">
          CSV uploader and Dotloop OAuth connection coming soon
        </p>
      </div>
    </div>
  );
}
